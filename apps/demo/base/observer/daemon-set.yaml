apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: observer
  namespace: orbservability
spec:
  selector:
    matchLabels:
      name: observer
  template:
    metadata:
      labels:
        name: observer
    spec:
      containers:
        - name: pixie
          image: TODO
        - name: observer
          image: TODO
      priorityClassName: system-node-critical
